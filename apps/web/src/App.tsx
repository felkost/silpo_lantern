// Scaffold only. The recovery card (Diagnosis+disclosure / Consent /
// Receipt) is built once apps/api has a session/SSE API for it to render.
function App() {
  return (
    <main>
      <h1>Ліхтарик</h1>
      <p>Scaffold stage — the recovery card is not built yet.</p>
    </main>
  );
}

export default App;
