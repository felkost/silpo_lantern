// Scaffold only (G1). The recovery card (Diagnosis+disclosure / Consent /
// Receipt, plan section 7.1) is built at G5+G6 once apps/api has a
// session/SSE API for it to render.
function App() {
  return (
    <main>
      <h1>Ліхтарик</h1>
      <p>Scaffold stage — the recovery card is not built yet.</p>
    </main>
  );
}

export default App;
