# Gate tooling only — never imported by application code.
# Updated 2026-09-05 alongside requirements.txt: resolved to the latest
# mutually-compatible versions and verified with `pip check` plus a full
# local gate run. mypy and pytest both jumped a major version in this pass
# (1.x->2.x, 8.x->9.x) — re-verified against this project's actual code
# after bumping, not assumed compatible from the version number alone.
black==26.5.1
flake8==7.3.0
mypy==2.3.1
pytest==9.1.1
pytest-asyncio==1.4.0
# G4 kickoff: randomizes test execution order every run (auto-enabled once
# installed, no addopts flag needed) so a hidden interdependency between two
# tests — one leaking state the other happens to rely on — fails loudly
# instead of staying invisible behind whatever order the files happen to be
# collected in. Prints its seed on every run; a failure caused by ordering
# reproduces exactly via `pytest -p randomly --randomly-seed=<printed seed>`.
pytest-randomly==5.0.0
# Pinned below 4.2.0 deliberately, matching the donor project's own measured
# finding: 4.2.0 inverted score polarity for several metrics (BiasMetric,
# HallucinationMetric, MisuseMetric, ToxicityMetric) on 2026-08-24. A later
# pip resolve here picked 4.2.1 by default (nothing in our own requirements
# constrains it below that) — pinned explicitly to the donor's verified-safe
# 4.1.10 instead of accepting whatever "latest" resolved to.
deepeval==4.1.10
Jinja2==3.1.6
# G3: mypy --strict stub packages for the two runtime deps
# src/lantern/policies/loader.py newly imports (PyYAML, jsonschema — both
# already pinned in requirements.txt). Dated stub releases matched to the
# pinned runtime versions (jsonschema==4.26.0) rather than "latest".
types-PyYAML==6.0.12.20260906
types-jsonschema==4.26.0.20260518
