# Pinned per agentic-project-kickoff step 6: "pin what you measure".
# Updated 2026-09-05: resolved to the latest mutually-compatible versions by
# letting pip pick them (mcp fixed at 1.29.0, everything else unconstrained),
# then verified with `pip check` (0 broken requirements) and a full local
# gate run inside a real venv built from this exact file — not asserted from
# memory. See insights.md for the two real mistakes this project already
# made by skipping that verification once.
#
# mcp stays pinned to 1.29.0 deliberately, not bumped to whatever is latest:
# `src/lantern/mcp/auth.py` (reused from the donor project) was verified
# against this exact version's API by direct source inspection
# (`TokenStorage` as a `typing.Protocol`, `OAuthClientInformationFull` shape).
# Bumping the SDK version without re-running that verification would silently
# invalidate it.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
mcp==1.29.0
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
langsmith==0.12.2
psycopg[binary]==3.3.5
sqlalchemy==2.0.52
httpx==0.28.1
python-dotenv==1.2.3
PyYAML==6.0.3
