# Pinned per agentic-project-kickoff step 6: "pin what you measure".
# Updated 2026-09-05: resolved to the latest mutually-compatible versions by
# letting pip pick them (mcp fixed at 1.29.0, everything else unconstrained),
# then verified with `pip check` (0 broken requirements) and a full local
# gate run inside a real venv built from this exact file — not asserted from
# memory. A dated internal log records the two real mistakes this project already
# made by skipping that verification once.
#
# mcp stays pinned to 1.29.0 deliberately, not bumped to whatever is latest:
# `src/lantern/mcp/auth.py` (reused from the donor project) was verified
# against this exact version's API by direct source inspection
# (`TokenStorage` as a `typing.Protocol`, `OAuthClientInformationFull` shape).
# Bumping the SDK version without re-running that verification would silently
# invalidate it.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
mcp==1.29.0
langgraph==1.2.11
# D-G4-01 (G4 kickoff): OpenRouter's LLM gateway (plan section 8.5), called
# through langchain-openai's ChatOpenAI with base_url pointed at OpenRouter
# — chosen over a raw httpx client (reimplements retry/tool-calling/
# structured-output already in the library) and the bare openai SDK
# (weaker native LangGraph node integration). Resolves cleanly against
# every other pin here (`pip check`: no broken requirements). The SAME
# commit that adds this pin also adds `langchain_openai`/`langchain_core`
# to test_layering.py's FORBIDDEN_FOR_DOMAIN_AND_SAFETY set (D-G4-06) —
# neither the existing "langchain" nor "openai" entries match this
# package's actual top-level module name.
langchain-openai==1.6.0
langgraph-checkpoint-postgres==3.1.2
langsmith==0.12.2
psycopg[binary]==3.3.5
sqlalchemy==2.0.52
httpx==0.28.1
python-dotenv==1.2.3
PyYAML==6.0.3
# Already installed transitively via mcp>=1.29.0 (mcp requires jsonschema>=4.20.0);
# pinned directly here because the fixture-validation pipeline (plan section 12.1.1)
# depends on it too, and a future mcp bump must not silently drop it out from under
# that unrelated part of the codebase (stage spec D-G1-05, round 2 correction).
jsonschema==4.26.0
