# Pinned per agentic-project-kickoff step 6: "pin what you measure".
# Corrected 2026-09-05 after CI caught a real dependency conflict on the
# first push: the first pass claimed these were "verified installed" against the donor
# project without actually diffing its requirements.txt. Only mcp, httpx,
# pydantic, and PyYAML are donor-measured pins (SupportFlow's own
# requirements.txt, 2026-08-25 kickoff review); mcp==1.29.0 requires
# pydantic>=2.11.0, which the donor's pydantic==2.13.4 satisfies and the
# first draft's 2.10.5 did not. fastapi, uvicorn, langgraph,
# langgraph-checkpoint-postgres, langsmith, psycopg, sqlalchemy, and
# python-dotenv are new to this project (the donor uses langfuse + chroma,
# not Neon + LangSmith) — verified only via `pip install --dry-run`
# resolving cleanly against this exact set, not against a real install.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
mcp==1.29.0
langgraph==0.5.3
langgraph-checkpoint-postgres==2.0.9
langsmith==0.3.5
psycopg[binary]==3.2.4
sqlalchemy==2.0.37
httpx==0.28.1
python-dotenv==1.0.1
PyYAML==6.0.3
