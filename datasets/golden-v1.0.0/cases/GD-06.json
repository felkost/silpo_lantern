{
  "case_id": "GD-06",
  "scenario": "Resume after the consent interrupt, twice: the bundle replays the REAL compiled graph through a first consented write that lands but leaves the blocker standing, then the second consent round, whose write the server reports as successful while the cart does not move -- so the read-back completes, disagrees, and the run ends honestly `unverified` rather than claiming a success no read-back confirmed. Re-cut onto a bundle carrying the tool-name fallback queue, so the case can also run under a live planner; the outcome shape is unchanged.",
  "gd_ref": "GD-06",
  "input": {
    "fixture_id": "replay_resume_unverified"
  },
  "expected_outcome": {
    "status": "unverified",
    "primary_code": "order.cost.min",
    "receipts_count": 2,
    "receipt_statuses": [
      "receipt",
      "unverified"
    ]
  },
  "mode": "replay",
  "source_id": "synthetic-bundle-g9-followup",
  "pii_status": "synthetic",
  "allowed_trajectories": [
    [
      "read",
      "diagnose",
      "compare_channels",
      "plan",
      "collect_and_gate",
      "rank",
      "explain",
      "write_guard",
      "write_and_readback",
      "persist_receipt"
    ]
  ],
  "allowed_tools": [
    "silpo_add_or_update_cart_products"
  ],
  "forbidden_tools": [
    "silpo_remove_cart_products"
  ],
  "rubric": "The graph pauses at the consent interrupt, resumes from a recorded consent without re-deriving it from an LLM summary, and a second round follows a verified-but-uncleared first round. The final outcome is reported honestly as unverified, never upgraded to a success."
}
